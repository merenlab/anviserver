/// <reference types="node" />
import { IncomingMessage, ServerResponse } from "http";
import { Options, Session, SessionRecord } from "./types";
export default function session<T extends SessionRecord = SessionRecord>(options?: Options): (req: IncomingMessage & {
    session?: Session<T> | undefined;
}, res: ServerResponse) => Promise<Session<T>>;
export type { Options, SessionStore } from "./types";
