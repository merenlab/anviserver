/// <reference types="node" />
import { ServerResponse } from "http";
import { Options, Session, SessionData } from "./types";
export declare function hash(sess: SessionData): string;
export declare function commitHeader(res: ServerResponse, name: string, { cookie, id }: Pick<Session, 'cookie' | 'id'>, encodeFn?: Options["encode"]): void;
