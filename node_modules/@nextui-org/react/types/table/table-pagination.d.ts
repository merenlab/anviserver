import React from "react";
import { CSS } from "../theme/stitches.config";
import { PaginationProps } from "../index";
import { NormalAlignment } from "../utils/prop-types";
interface Props {
    animated?: boolean;
    rowsPerPage?: number;
    align?: NormalAlignment;
    onPageChange?: PaginationProps["onChange"];
}
declare type NativeAttrs = Omit<React.HTMLAttributes<unknown>, keyof Props | "onChange">;
export declare type TablePaginationProps = Props & NativeAttrs & Omit<Partial<PaginationProps>, "onChange"> & {
    css?: CSS;
};
declare const TablePagination: React.FC<TablePaginationProps>;
export default TablePagination;
